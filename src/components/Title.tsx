import {Typography} from "@mui/material";

const Title = () =>{
    return(
        <Typography variant={'h1'} sx={{my:"1.5rem",color:"primary.main"}}>Todo list</Typography>
    )
}
export default Title
